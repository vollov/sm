from settings import *

DEBUG = TEMPLATE_DEBUG = CAPTCHA_TEST_MODE = False
ALLOWED_HOSTS = ['127.0.0.1', 'localhost', '.jennyli.ca']

# the number of seconds the cookie can live for
#SESSION_COOKIE_AGE=1209600
# Two weeks
#SESSION_EXPIRE_AT_BROWSER_CLOSE=False
